# Reports

Everything measured, in one place. Root-level `README_pusht.md` is how to RUN the arms; this
folder is what happened when they were run.

| page | answers |
|---|---|
| [RESULTS.md](RESULTS.md) | **Do these methods work on PushT?** Every arm, one table, with the caveats that decide whether two rows can be compared. Start here. |
| [MANIFEST.md](MANIFEST.md) | What is not in this repo, where it lives, and why it was left out. |
| [tgoal_moving_arms_2026-09-19.md](tgoal_moving_arms_2026-09-19.md) | **Does training only on the transitions `t_goal` can see change how the policy uses its search context?** Eleven arms: 2 widths x {uniform, flat400, ramp400to200, flat600, ramp600to200} plus BC. The obs-noise ladder drives context use, search gain and success rate, and the effect scales with the corruption level. §7 adds a five-step checkpoint sweep (20k-100k, argmax and final_pass, clean and each ladder arm's own conditional) -- no checkpoint is nominated, and across every step the verifier ranks undirected random samples above all eleven arms and above a*. |
| [ppo_sac_lstm-bc_eval_2026-09-17.md](ppo_sac_lstm-bc_eval_2026-09-17.md) | **Can a learned V or Q replace the best-of-N heuristic?** In progress: the heuristic baseline and both BC stages are measured; no learned value is evaluated yet. |
| [sac_per_split_q_2026-09-20.md](sac_per_split_q_2026-09-20.md) | **Does the best-of-N result survive a Q that never saw the geometric test episodes?** In progress: two SAC keypoint arms seeded only from blq137's and brd100's own train splits, so each manifest's 50 test episodes are held out by construction rather than by argument. Live readout, regenerated by `scripts/sac_split_status.py`. |
| [q_fn_sac_all_206_demos_2026-09-20.md](q_fn_sac_all_206_demos_2026-09-20.md) | **Does the `sac_all` Q rank best-of-N, and does training ST against it help?** Ranking: no — `t_goal` gains +0.12…+0.68 with n on all four UNet BC rows and the Q gains nothing on any, with the n=1 leak check passing 4/4. Context: also no — across all 12 Q-trained ST arms at 100k, `argmax` beats the verifier-OFF control on 7 and loses on 5, i.e. selecting by Q is indistinguishable from not selecting. Tag `q_fn_sac_all_206_demos`; nothing here is held out from the Q. |
| [q_verifier_2026-09-19.md](q_verifier_2026-09-19.md) | **Can the SAC Q replace the best-of-N heuristic?** No, and the two halves disagree: Q ranks the expert action 4-9x better than `t_goal` where `t_goal` is blind, with non-overlapping intervals — but `t_goal` wins the best-of-N sweep on all six rows. Ranking `a*` is necessary, not sufficient. |
| [DEAD_CODE.md](DEAD_CODE.md) | Modules and hydra targets with no live caller, recorded rather than deleted. |
| [`from-modes-figures/`](from-modes-figures/) | **Shortcut to the mode-analysis figures** for `tgoal_moving_arms_2026-09-19.md` §6 — 50 PNGs, all eleven arms at step 100k. A symlink to `/gscratch/.../mode_analysis/figures_tgoal_moving/`, because 9 MB of regenerable PNGs do not belong in the history; rebuild with `bash scripts/collect_mv_figures.sh`. |
| `summaries/` | Run provenance (`run.json`, `splits.json`) and selection curves for every run cited above — 520 files, ~2 MB. Subdirectories are `from-<source>/`; see MANIFEST.md for why the prefix is load-bearing. |
| `ppo/` | Generated by `recurrent_ppo/scripts/runs_doc.py`. **Empty right now** — the previous 18-run generation was deleted on 2026-09-16 and the replacement arms are still training. |
| `archive/` | 24 dated readouts, moved here verbatim on 2026-09-16 and not edited. |

## About `archive/`

These are lab-notebook entries, not current documentation: each was true on its date and is
kept because the measurements in it were real. Two things to know before citing one.

`LATEST_SUCCESS_RATES.md` is named "latest" but was last written **2026-08-22**, and covers
neither the LSTM BC arms (trained 2026-09-15), nor PPO, nor SAC. `RESULTS.md` supersedes it.

Several of these docs cite a `SUCCESS_RATES.md` that was **deleted on 2026-08-30** along with
`SUCCESS_RATES_30_100.md` — 6,428 lines between them. References in live code now point at
`docs/reports/SUCCESS_RATES.md`, which `scripts/build_success_rates_doc.py` regenerates; the
references inside `archive/` were deliberately left pointing at the old name, because rewriting
a dated record to cite a document that did not exist when it was written would falsify it.

Most files here are generated. `scripts/build_*_doc.py` writes them, and each now writes into
`archive/` rather than the repo root.
