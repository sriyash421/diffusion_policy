# maze search -- obs concatenated onto every token. Both projections halve to hidden_dim//2,
# so hidden_dim doubles to keep the token width. Everything else inherits maze_search.
defaults:
  - maze_search
  - _self_

name: maze_search_concat_obs

policy:
  concat_obs: True
  hidden_dim: 512
