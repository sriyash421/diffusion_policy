# maze search -- corruption ablation. Everything else inherits maze_search.
defaults:
  - maze_search
  - _self_

name: maze_search_corruption

policy:
  corrupt_obs: True
  # training augmentation only; eval rollouts see clean obs
  corrupt_obs_eval: False
