# maze search -- mask_obs + corruption. Everything else inherits maze_search.
defaults:
  - maze_search
  - _self_

name: maze_search_mask_obs_corruption

policy:
  mask_obs: True
  corrupt_obs: True
  corrupt_obs_eval: False
