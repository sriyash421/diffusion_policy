# maze search -- first candidate bypasses the trunk via first_action_mlp.
# Everything else inherits maze_search.
defaults:
  - maze_search
  - _self_

name: maze_search_mask_obs

policy:
  mask_obs: True
