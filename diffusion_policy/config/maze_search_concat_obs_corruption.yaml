# maze search -- concat_obs + corruption. Everything else inherits maze_search.
defaults:
  - maze_search
  - _self_

name: maze_search_concat_obs_corruption

policy:
  concat_obs: True
  hidden_dim: 512
  corrupt_obs: True
  corrupt_obs_eval: False
